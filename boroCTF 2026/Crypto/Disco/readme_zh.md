# Disco

## 題目描述

The hexagonal colors are simply beautiful.

## 解題思路

題目給了一張圖片，把 12 個色塊轉成 RGB hex，然後再把每個 byte 轉成 ascii，就可以得到 flag 了。

後面有一些是 `#000000` 的色塊可以直接忽略，不影響 flag。

## Flag

```text
boroCTF{nEv3r_l0$e_YoU4_Be@t}
```
